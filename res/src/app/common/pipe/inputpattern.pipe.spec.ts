import { InputpatternPipe } from './inputpattern.pipe';

describe('InputpatternPipe', () => {
  it('create an instance', () => {
    const pipe = new InputpatternPipe();
    expect(pipe).toBeTruthy();
  });
});
